build:
  nodes:
    coverage:
      tests:
        override: [js-scrutinizer-run]
          - command: npm run test-scrutinizer
            coverage:
              file: coverage/clover.xml
              format: clover
